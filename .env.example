# ── Supabase (orders persistence + storefront⇄admin sync) ──────────────────
# Leave these unset to run in DEMO mode (in-memory sample data, resets on
# restart). Set them (and run supabase/schema.sql) to go live with real data.
NEXT_PUBLIC_SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=

# ── Stripe (real payments) — wired in the next step ────────────────────────
# STRIPE_SECRET_KEY=
# STRIPE_WEBHOOK_SECRET=
# NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=
